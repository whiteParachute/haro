{
  "name": "@haro/providers",
  "version": "0.1.0",
  "description": "Haro Provider implementations (placeholder — concrete providers arrive in FEAT-002 / FEAT-003)",
  "engines": {
    "node": ">=22.0.0"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@haro/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
