{
  "name": "@haro/channel",
  "version": "0.0.0",
  "private": true,
  "description": "Haro shared channel abstraction and registry (FEAT-008)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "@clack/prompts": "^0.10.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.18.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
