{
  "name": "@haro/provider-claude",
  "version": "0.0.0",
  "private": true,
  "description": "Haro Claude Provider — wraps @anthropic-ai/claude-agent-sdk (FEAT-002)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:live": "vitest run --mode live --testNamePattern @live"
  },
  "dependencies": {
    "@haro/core": "workspace:*",
    "@anthropic-ai/claude-agent-sdk": "^0.2.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
